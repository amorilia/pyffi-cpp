fileformat TEST

class UInt
class Float
class String

class Key:
    abstract UInt interpolation
    abstract String description

    Float time(default=0.0)
    UInt value(default=1)

class InterpolatedKey:
    String description(default="hello world")
    UInt interpol(default=3)
    Key key(interpolation=interpol,description=description)

