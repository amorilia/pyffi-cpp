fileformat TEST

class UInt

# fields declared at global scope are arguments required for parsing
# the binary file (typically, version numbers)
abstract UInt version
abstract UInt user_version
abstract UInt user_version_2

class Key:
    # also test embedded type declarations
    class Float

    abstract UInt interpolation

    Float time
    UInt value

